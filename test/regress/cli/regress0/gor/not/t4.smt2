; COMMAND-LINE: --produce-models
; EXPECT: sat
; EXPECT: (
; EXPECT: (define-fun x () Int 0)
; EXPECT: (define-fun y () Int 1)
; EXPECT: (define-fun z () Int 2)
; EXPECT: (define-fun a () Int 3)
; EXPECT: (define-fun b () Int 4)
; EXPECT: (define-fun w () Int 5)
; EXPECT: (define-fun t () Int 6)
; EXPECT: (define-fun p () Int 7)
; EXPECT: (define-fun q () Int 8)
; EXPECT: )
; EXIT: 0

(set-logic ALL)
(set-info :status sat)
(declare-const x Int)
(declare-const y Int)
(declare-const z Int)
(declare-const a Int)
(declare-const b Int)
(declare-const w Int)
(declare-const t Int)
(declare-const p Int)
(declare-const q Int)
(assert (gor.< x y))
(assert (gor.< y z))
(assert (gor.< y a))
(assert (gor.< x b))
(assert (gor.< y b))
(assert (gor.< b z))
(assert (gor.< a b))
(assert (gor.< w t))
(assert (gor.< w p))
(assert (gor.< q w))
(assert (gor.< q p))
(assert (gor.< q y))
(assert (gor.< z p))
(assert (gor.< t a))
(assert (gor.< q p))
(assert (gor.< x q))
(assert (gor.< w b))

(assert (not (gor.< b w)))
(assert (not (gor.< z q)))
(assert (not (gor.< b w)))
(assert (not (gor.< p z)))
(assert (not (gor.< t y)))
(assert (not (gor.< y t)))
(assert (not (gor.< b q)))
(assert (not (gor.< b t)))
(check-sat)
(get-model)
